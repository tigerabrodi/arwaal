import { createElement } from './core/createElement'
import { useCallback, useEffect, useMemo, useRef, useState } from './core/hooks'
import { render } from './core/render'

export const Arwaal = {
  createElement,
  render,
  useEffect,
  useState,
  useRef,
  useMemo,
  useCallback,
}
